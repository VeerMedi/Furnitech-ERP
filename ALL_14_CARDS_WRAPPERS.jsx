// READY-TO-USE WRAPPER CODE FOR ALL 14 REMAINING CARDS
// Copy each block and replace the corresponding card in Dashboard.jsx

// ==================== CARD: Active Salesmen ====================
{
    enabledCards.includes('active-salesmen') && (
        <DraggableCardWrapper
            cardId="active-salesmen"
            cardTitle="Active Salesmen"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content here - just remove outer div className */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Production Pending ====================
{
    enabledCards.includes('production-pending') && (
        <DraggableCardWrapper
            cardId="production-pending"
            cardTitle="Production Pending"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Monthly Revenue ====================
{
    enabledCards.includes('monthly-revenue') && (
        <DraggableCardWrapper
            cardId="monthly-revenue"
            cardTitle="Monthly Revenue"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Today's Revenue ====================
{
    enabledCards.includes('todays-revenue') && (
        <DraggableCardWrapper
            cardId="todays-revenue"
            cardTitle="Today's Revenue"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Total Customers ====================
{
    enabledCards.includes('total-customers') && (
        <DraggableCardWrapper
            cardId="total-customers"
            cardTitle="Total Customers"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Active Inquiries ====================
{
    enabledCards.includes('active-inquiries') && (
        <DraggableCardWrapper
            cardId="active-inquiries"
            cardTitle="Active Inquiries"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Pending Orders ====================
{
    enabledCards.includes('pending-orders') && (
        <DraggableCardWrapper
            cardId="pending-orders"
            cardTitle="Pending Orders"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Low Stock Alerts ====================
{
    enabledCards.includes('low-stock-alerts') && (
        <DraggableCardWrapper
            cardId="low-stock-alerts"
            cardTitle="Low Stock Alerts"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Raw Materials Status ====================
{
    enabledCards.includes('raw-materials-status') && (
        <DraggableCardWrapper
            cardId="raw-materials-status"
            cardTitle="Raw Materials Status"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Machine Utilization ====================
{
    enabledCards.includes('machine-utilization') && (
        <DraggableCardWrapper
            cardId="machine-utilization"
            cardTitle="Machine Utilization"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Maintenance Alerts ====================
{
    enabledCards.includes('maintenance-alerts') && (
        <DraggableCardWrapper
            cardId="maintenance-alerts"
            cardTitle="Maintenance Alerts"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Pending Deliveries ====================
{
    enabledCards.includes('pending-deliveries') && (
        <DraggableCardWrapper
            cardId="pending-deliveries"
            cardTitle="Pending Deliveries"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: Completed Deliveries ====================
{
    enabledCards.includes('completed-deliveries') && (
        <DraggableCardWrapper
            cardId="completed-deliveries"
            cardTitle="Completed Deliveries"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}

// ==================== CARD: In Transit ====================
{
    enabledCards.includes('in-transit') && (
        <DraggableCardWrapper
            cardId="in-transit"
            cardTitle="In Transit"
            onDragStart={handleDragStart}
            onDragEnd={handleDragEnd}
        >
            {/* Keep existing card content */}
        </DraggableCardWrapper>
    )
}
